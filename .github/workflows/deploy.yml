name: Deploy CCFV

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Cloudflare environment to deploy
        required: true
        type: choice
        options:
          - staging
          - production

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Verify application
        run: pnpm check && pnpm test
      - name: Build Worker for selected environment
        env:
          CLOUDFLARE_ENV: ${{ inputs.environment }}
        run: pnpm build
      - name: Apply D1 migrations
        shell: bash
        run: |
          if [ "${{ inputs.environment }}" = "staging" ]; then
            pnpm db:migrate:staging
          else
            pnpm db:migrate:production
          fi
      - name: Deploy Worker
        shell: bash
        run: |
          if [ "${{ inputs.environment }}" = "staging" ]; then
            WORKER_NAME="ccfv"
          else
            WORKER_NAME="ccfv-production"
          fi
          pnpm exec wrangler deploy --name "$WORKER_NAME" --var "PUBLIC_TURNSTILE_SITE_KEY:${TURNSTILE_SITE_KEY}"
